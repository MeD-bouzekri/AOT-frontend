import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,

};

module.exports = {
  allowedDevOrigins: ['lakesha-preeditorial-marlene.ngrok-free.dev'],
}

export default nextConfig;
